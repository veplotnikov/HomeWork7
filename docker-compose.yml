version: '3'
volumes:
  data-vol:
    external: true

services:
  builder:
    build: builder
    volumes:
      - "data-vol:/opt"

  tomcat:
    build: service
    ports: 
      - "8080:8080"
    volumes:
      - "data-vol:/opt"
